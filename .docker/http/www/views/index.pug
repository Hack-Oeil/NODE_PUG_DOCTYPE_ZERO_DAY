extends layout.pug 

block content 
    section.container.mx-auto.max-w-3xl.bg-white.shadow-md.rounded-xl.p-8.mt-8
      h1.text-2xl.font-bold.text-gray-800.mb-6 📧 Rédiger un Mail

      form(action="./send" method="POST" class="space-y-6")
        // Champ "De"
        div
          label.block.text-sm.font-medium.text-gray-700.mb-1 De
          input(
            type="email"
            name="from"
            required
            placeholder="votre.email@exemple.com"
            class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-400"
          )

        // Champ "À"
        div
          label.block.text-sm.font-medium.text-gray-700.mb-1 À
          input(
            type="email"
            name="to"
            required
            placeholder="destinataire@domaine.com"
            class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-400"
          )

        // Champ "Objet"
        div
          label.block.text-sm.font-medium.text-gray-700.mb-1 Objet
          input(
            type="text"
            name="subject"
            required
            placeholder="Sujet du mail"
            class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-400"
          )

        // Champ "Message"
        div
          label.block.text-sm.font-medium.text-gray-700.mb-1 Message
          textarea(
            name="message"
            required
            rows="8"
            placeholder="Écrivez votre message ici..."
            class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-400 resize-none"
          )

        // Bouton d'envoi
        div.text-right
          button(
            type="submit"
            class="bg-blue-600 hover:bg-blue-700 text-white font-semibold px-6 py-2 rounded-lg transition duration-200"
          ) 📤 Envoyer le Mail